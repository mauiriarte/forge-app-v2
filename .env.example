# Supabase project "Forger" — the publishable/anon key is safe to expose client-side.
# Copy to .env and fill in the key (Supabase dashboard → Settings → API keys),
# then rebuild: the app switches from local-only mode to auth + cloud sync.
VITE_SUPABASE_URL=https://ydbojinuugfvwkfgyswl.supabase.co
VITE_SUPABASE_ANON_KEY=
